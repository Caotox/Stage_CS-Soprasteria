<obj id="pocConfiguration.IDSCPSettings.{{nomItem}}" class-name="pocConfiguration.IDSCPSettings" nom="{{nomItem}}">
	  <fv nom="commentaire" val="{{item.commentaire}}" />
	  <fv nom="dscpAudio" enum="{{item.dscpAudio}}" />

	  <fv nom="dscpDefault" enum="{{item.dscpDefault}}" />

	  <fv nom="dscpSip" enum="{{item.dscpSip}}" />

	</obj>
	